<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">

    <%= f.text_field :fullname, class:"input-block-level", :placeholder => "Full Name"%>
  </div>
  <div class="field">

    <%= f.text_field :email, class:"input-block-level", :placeholder => "Email"%>
  </div>
  <div class="field">

    <%= f.text_field :subject, class:"input-block-level", :placeholder => "Subject"%>
  </div>
  <div class="field">

    <%= f.text_area :comment, class:"input-block-level height-200", :placeholder => "Comment", :rows => "8"%>
  </div>
  <div class="actions">
    <%= f.submit "Send", class: "btn btn-info pull-right" %>
  </div>
<% end %>
